include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # tool/screenshots — это тест, просто лежит вне каталога test/
    invalid_use_of_visible_for_testing_member: ignore
  exclude:
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
